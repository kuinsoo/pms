<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.or.ddit.schedule.mapper.ScheduleMapper">
	<select id="myProjectList" resultType="scheduleVo" parameterType="String">
		SELECT	p.project_id,
				p.project_title
		FROM    project_member pm INNER JOIN project p
		ON      pm.pmember_project = p.project_id
		WHERE   pm.pmember_member = #{sid}
	</select>
</mapper>
